node_modules/
dist/
*.tsbuildinfo
.env
.env.local
.cache/
# Recursive: a generated project nests its own build output a few levels down.
**/node_modules/
**/.next/
**/out/
# Everything a migration produces. It is output, not source: every file under here is
# regenerated by `underpin build`, and most of it is a copy of a website we do not own.
sites/
.underpin/
CLAUDE.md
# Working notes and correspondence, not project documentation.
docs/reply-to-send.txt
docs/fidelity-brief-for-architect.md
docs/componentized-fidelity-brief.md
.shots/
